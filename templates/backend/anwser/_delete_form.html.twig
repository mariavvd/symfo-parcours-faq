<form method="post" action="{{ path('backend_anwser_delete', {'id': anwser.id}) }}" onsubmit="return confirm('Are you sure you want to delete this anwser?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ anwser.id) }}">
    <button class="btn">Delete</button>
</form>
